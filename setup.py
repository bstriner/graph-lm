from setuptools import setup, find_packages

setup(name='graph-lm',
      version='0.0.1',
      #install_requires=['tensorflow'],
      author='Ben Striner',
      url='https://github.com/bstriner/graph-lm',
      packages=find_packages())
